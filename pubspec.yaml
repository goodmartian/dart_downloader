name: dart_downloader
description: A simple BLoC interface to download files.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  http: ^0.13.3
  collection: ^1.15.0
  bloc: ^7.0.0
  equatable: ^2.0.2

dev_dependencies:
  pedantic: ^1.11.0
